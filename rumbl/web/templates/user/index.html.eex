<h1>Listing Users</h1>
<table class="table">
<%= for user <- @users do %>
    <tr>
        <td> <%= user.id %> </td>
        <td> <%= first_name(user) %> </td>
        <td> <%= link "View", to: user_path(@conn, :show, user.id) %> </td>
    </tr>
<% end %>
</table>
